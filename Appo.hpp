#ifndef APPO
#define APPO
#include <string>
using namespace std;
//sets a struct appointment which include hours mins and patient name
struct Appointment{
public:
    int hours;
    int mins;
    string patname;
};
#endif /* Appo_h */
